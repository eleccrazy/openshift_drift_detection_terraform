## Openshift Drift Detection with Terraform.

This repository contains Terraform code to set up a drift detection baseline for an OpenShift cluster. The baseline is defined in a YAML file and includes rules to check for specific conditions in the cluster, such as ensuring that worker nodes are not cordoned.